galen config
is
Created config file
------

galen test home.gspec -Dwebdriver.chrome.driver=/opt/chromedriver/chromedriver

galen check home.gspec --url http://testapp.galenframework.com/ --size 1024x768 --htmlreport reports

galen test home.gspec --url http://testapp.galenframework.com/


before_install:
  - sudo apt-get install openjdk-7-jdk
  - java -version
  - mvn -version
  - phantomjs --version
  - npm install -g galenframework-cli
  - npm install -g grunt-cli